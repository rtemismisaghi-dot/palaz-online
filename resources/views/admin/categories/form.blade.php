@extends('layouts.admin') @section('content')<h1 class="mb-4">{{ $category->exists ? 'ویرایش دسته‌بندی' : 'دسته‌بندی جدید' }}</h1><form method="post" action="{{ $category->exists ? route('admin.categories.update',$category) : route('admin.categories.store') }}" class="card p-4">@csrf @if($category->exists) @method('PUT') @endif<div class="row g-3"><div class="col-md-6"><label class="form-label">نام</label><input name="name" class="form-control" value="{{ old('name',$category->name) }}" required></div><div class="col-md-6"><label class="form-label">Slug / کد</label><input name="slug" class="form-control" value="{{ old('slug',$category->slug) }}" required></div><div class="col-md-6"><label class="form-label">عنوان کوچک</label><input name="eyebrow" class="form-control" value="{{ old('eyebrow',$category->eyebrow) }}"></div><div class="col-md-3"><label class="form-label">Tone</label><input name="tone" class="form-control" value="{{ old('tone',$category->tone) }}"></div><div class="col-md-3"><label class="form-label">ترتیب</label><input type="number" min="0" name="sort_order" class="form-control" value="{{ old('sort_order',$category->sort_order ?? 0) }}"></div><div class="col-12 form-check form-switch mx-2"><input class="form-check-input" type="checkbox" name="is_active" value="1" {{ old('is_active',$category->exists ? $category->is_active : true) ? 'checked' : '' }}><label class="form-check-label">فعال</label></div></div><div class="mt-4 d-flex gap-2"><button class="btn btn-palaz">ذخیره</button><a class="btn btn-light" href="{{ route('admin.categories.index') }}">انصراف</a></div></form>@endsection