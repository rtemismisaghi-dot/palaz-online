@extends('layouts.admin') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1>محصولات</h1><p class="text-secondary mb-0">قیمت پایه و منطق محاسبه از همین‌جا کنترل می‌شود.</p></div><a class="btn btn-palaz" href="{{ route('admin.products.create') }}">+ محصول جدید</a></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>محصول</th><th>دسته</th><th>قیمت پایه</th><th>واحد</th><th>محاسبه</th><th>وضعیت</th><th></th></tr></thead><tbody>@forelse($products as $product)<tr><td>{{ $product->name }}<small class="d-block text-secondary">{{ $product->slug }}</small></td><td>{{ $product->category?->name }}</td><td>{{ $product->price !== null ? number_format($product->price) : 'تماس' }}</td><td>{{ $product->unit }}</td><td>{{ $product->pricingRule?->calculation_type ?? 'ثبت نشده' }} @if($product->pricingRule) · {{ $product->pricingRule->waste_percent }}٪ پرت @endif</td><td>{{ $product->is_active ? 'فعال' : 'غیرفعال' }}</td><td><a href="{{ route('admin.products.edit',$product) }}">ویرایش</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5">هنوز محصول ثبت نشده.</td></tr>@endforelse</tbody></table></div></div>@endsection